# 本次执行环境记录

- 2026-09-20：受限环境启动 `node node_modules/tsx/dist/cli.mjs server/main.ts` 时 esbuild 子进程报 `spawn EPERM`。同一命令以限定进程启动权限重跑；不是应用业务错误。已有命令记录和历史截图保持原样。
- 2026-09-20：并行编辑触发 Vite 页面重载，后续浏览器操作遇到已隐藏的远程路径输入框并超时。验收前重新打开目标窗口，避免复用重载前的 UI 状态。
- 2026-09-20：浏览器验收使用过期按钮名称“保存到服务器”超时，实际控件为“保存设置”；后续按当前组件与 DOM 定位。
- 2026-09-20：浏览器全文确认测试同时使用 page.once(dialog) 与工具弹窗处理造成重复 dismiss；改为单独使用 browser_handle_dialog。右键测试首次选中的是表头，改为真实数据行 index 单元格。
- 2026-09-20：并行编辑验收脚本会触发开发服务器刷新；最终浏览器验收切换 production 实例。历史/AI 数据目录已加入 Vite ignored，保存数据本身不再刷新页面。
- 2026-09-20：恢复多轮对话后，单个模拟回复文字匹配多个节点；浏览器验收改用 `.last()`，不重复发送请求。
