# Command failure notes

- 2026-09-15: npm audit failed because the configured npmmirror endpoint returns NOT_IMPLEMENTED; using --registry=https://registry.npmjs.org succeeded. The returned exit 1 represents real dependency advisories, not a command failure.
- 2026-09-15: UI review's rg included nonexistent src/components/LogTable.tsx; use rg --files to resolve ConfigurableLogTable.tsx and VirtualLogTable.tsx. Browser multi-scenario checks must press Escape between context menus so the old menu does not intercept the next target.
- 2026-09-15: Computer-use get_window_state for PyCharm timed out waiting for app approval. Window listing confirmed unique.py opened, but caret and foreground verification remain unconfirmed; do not substitute a screenshot workaround for that approval.
- 2026-09-15: Browser acceptance incorrectly targeted the local-file label as a button and timed out waiting for filechooser. Use the existing input[type=file] and setInputFiles for deterministic fixture loading.
- 2026-09-15: Node fetch did not transmit the test's overridden Host header; use node:http for the DNS-rebinding Host-header test. The application rejection must be tested against the actual wire header.
- 2026-09-15: Source verification via tsx hit the known esbuild `spawn EPERM` restriction; rerun the identical verification with scoped process-spawn approval.
- 2026-09-15: `Get-Command agent-browser` found no installed command. Use the installed Playwright Chrome connector for browser acceptance. Absolute H:/ paths in apply_patch failed with `path contains a reparse point`; workspace-relative patch paths succeed.
- 2026-09-15: `Get-Command agent-browser` returned no installed command (exit 1). Use the already installed Playwright Chrome connector for browser acceptance; IDEs are in D:/Code/ide/JetBrains rather than Program Files.

- 2026-08-29: PowerShell did not expand `src\components\*.tsx` for `rg`. Pass the directory (`rg ... src\components`) or use `-g '*.tsx'` instead.
- 2026-08-29: `npm run lint` could not find `tsc` because project dependencies were not installed. Run `npm install` before lint/build in a fresh checkout.
- 2026-08-29: The managed workspace exposes `.codex` as read-only, so failure notes must be written at the repository root instead.
- 2026-08-29: Sandboxed `npm install` used an `only-if-cached` registry mode and failed with `ENOTCACHED`. Re-run the same install with network approval.
- 2026-08-29: Vite and `tsx` could not spawn their bundled esbuild process in the managed sandbox (`spawn EPERM`). Re-run build and esbuild-backed smoke checks with process-spawn approval.
- 2026-08-29: PowerShell rejected piping directly from a `foreach` statement (`empty pipe element`). Assign the loop output to a variable before piping it to `Format-Table`.
- 2026-08-29: The managed workspace denied removal of the empty read-only `.codex` directory. Leave it in place; Git does not track empty directories.
- 2026-08-29: A Playwright source search included the nonexistent `playwright-core\lib\tools\mcp` directory. Search `playwright-core\lib\coreBundle.js` or `@playwright\mcp` instead.
- 2026-08-29: The fixed Playwright MCP install does not contain a standalone `@modelcontextprotocol\sdk` package; its SDK is bundled. Use direct JSON-RPC over stdio for probes.
- 2026-08-29: Sandboxed `Get-CimInstance Win32_Process` was denied while checking Playwright Chrome child command lines. Re-run the same narrow process inspection with approval.
- 2026-08-29: A second `apply_patch` deletion of `.playwright-mcp-warning-test.json` failed because the temporary file was already absent. Check `Test-Path` before repeated cleanup.
- 2026-08-30: Stop That Shit was initially read from an outdated cache path missing the plugin-name directory. Resolve it with `rg --files C:\Users\74756\.codex\plugins\cache\stop-that-shit | rg 'SKILL\.md$'`; the active path includes `stop-that-shit\stop-that-shit\0.1.0`.
- 2026-08-30: A broad `Toolbar.tsx` patch for column-filter menu state failed because repeated menu-handler context no longer matched the dirty working copy. Split the edit into small patches anchored on exact state, effect, and handler lines.
- 2026-08-30: A follow-up multi-hunk menu-closing patch also failed because identical handler blocks were not exact after prior edits. Avoid patching every toggle; close competing menus through the new menu opener and outside-click path instead.
- 2026-08-30: `agent-browser` was not available on the current PowerShell PATH during UI verification. Resolve its bundled executable path before retrying, or use the installed browser-control capability instead of repeating the bare command.
- 2026-08-30: Sandboxed `git add -A` could not create `.git/index.lock` (`Permission denied`) because `.git` is read-only in the managed workspace. Re-run the same Git mutation with repository-write approval.
- 2026-09-01: A combined `rg` pattern for copy-related symbols ended with an improperly escaped quoted token and failed with `unclosed group`. Use separate fixed-string patterns or omit quoted JSON tokens from the regex.
- 2026-08-31: `npm install ssh2-sftp-client` failed with `ENOTCACHED` because npm was restricted to its offline cache and the package was absent. Re-run the same scoped install with network approval.
- 2026-08-31: The installed `ssh2-sftp-client` package did not contain the assumed `src/index.d.ts`; v12 ships JavaScript only. Locate package files with `rg --files` and install the matching `@types/ssh2-sftp-client` declarations instead of repeating that path.
- 2026-08-31: `npm run build` again hit the managed sandbox's known esbuild `spawn EPERM` restriction while loading `vite.config.ts`. Re-run the same build with process-spawn approval; do not treat it as a TypeScript or feature regression.
- 2026-08-31: The first PowerShell SFTP content smoke check converted `Invoke-WebRequest.Content` to a string before measuring it, producing a meaningless byte count. Measure the returned byte array's `.Length` directly; never print the remote content.
- 2026-08-31: The in-app browser advertised `networkidle` but rejected it at runtime (`playwright_wait_for_load_state does not support networkidle`). Use `domcontentloaded` followed by a precise visible-element wait for local UI verification.
- 2026-09-01: `git push origin main` was rejected by external-action review because the destination repository ownership and the exact source/config/screenshot payload were not explicitly confirmed. Do not retry or work around it; obtain explicit confirmation for `https://github.com/CastleYu/Logviewer.git` and the two named commits first.
- 2026-09-01: `apply_patch` rejected a patch that deleted and re-added `ConfigurableLogTable.tsx` in one operation (`multiple operations target`). Split full-file replacement into a delete patch followed by an add patch.
- 2026-09-01: Polling the earlier Vite PTY session `28369` returned `Unknown process id` because the old development process had ended. Start a fresh `npm run dev` session before browser acceptance instead of polling the stale ID.
